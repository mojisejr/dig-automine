# Environment variables for AutoMine Project

# ------------------------------------------------------------------------------
# Frontend (packages/frontend)
#
# These variables are used by the Next.js application for connecting to the
# blockchain and interacting with the smart contract.
# ------------------------------------------------------------------------------

# The URL of the RPC endpoint for the blockchain network (e.g., Bitkub Chain).
NEXT_PUBLIC_RPC_URL="https://rpc.bitkubchain.io"
NEXT_PUBLIC_RPC_URL_TESTNET="https://rpc-testnet.bitkubchain.io"

# The address of the deployed AutoMine smart contract.
NEXT_PUBLIC_AUTOMINE_CONTRACT_ADDRESS="0x..."

# ------------------------------------------------------------------------------
# Backend & Bot (packages/bot)
#
# These variables are used by the backend services and the automated bot for
# database connections, blockchain interactions, and other services.
# ------------------------------------------------------------------------------

# The connection string for the Render Postgres database.
DATABASE_URL="postgresql://user:password@host:port/database"

# The private key of the wallet that the bot will use to send transactions.
# IMPORTANT: Do not commit this key to version control.
BOT_PRIVATE_KEY="0x..."

# The address of the deployed AutoMine smart contract (can be the same as the frontend).
AUTOMINE_CONTRACT_ADDRESS="0x..."

# The URL of the RPC endpoint for the blockchain network (can be the same as the frontend).
RPC_URL="https://rpc.bitkubchain.io"
RPC_URL_TESTNET="https://rpc-testnet.bitkubchain.io"

# ------------------------------------------------------------------------------
# Smart Contract Deployment (packages/contracts)
#
# These variables are used by Hardhat for deploying the smart contracts.
# ------------------------------------------------------------------------------

# The private key of the account that will be used to deploy the contracts.
# IMPORTANT: Do not commit this key to version control.
DEPLOYER_PRIVATE_KEY=""

# The RPC URL for the network where the contracts will be deployed (e.g., Bitkub Chain).
DEPLOY_RPC_URL="https://rpc.bitkubchain.io"